apiVersion: v1
kind: Secret
metadata:
  name: mcp-secrets
  namespace: mcp-platform
type: Opaque
stringData:
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "Bhushan-123" # <-- replace with real RDS password
  JWT_SECRET: "" # <-- replace with real secret
  OPENAI_API_KEY: "" # <-- replace with real OpenAI API key
  REDIS_PASSWORD: "" # leave empty if no Redis auth
  